import { FilterByUnicodePipe } from './filter-by-unicode.pipe';

describe('FilterByUnicodePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByUnicodePipe();
    expect(pipe).toBeTruthy();
  });
});
